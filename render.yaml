services:
  - type: web
    name: むさしの号について
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: BP_CPYTHON_VERSION
        value: 3.12.*
